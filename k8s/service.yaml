apiVersion: v1
kind: Service
metadata:
  name: ai-dev-asst-client
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: ai-dev-asst-client
  ports:
    - name: http
      port: 80
      targetPort: http
